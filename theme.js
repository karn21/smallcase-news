import {StyleSheet} from 'react-native';

const theme = StyleSheet.create({
  container: {
    flex: 1,
  },
});

const colors = {
  primary: '#f86730',
  primaryDeep: '#f74f0f',
  primaryLight: '#FDAC8E',
};

export {colors};

export default theme;
